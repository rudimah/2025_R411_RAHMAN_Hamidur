name: demineur
description: Un projet Flutter pour le jeu Démineur.

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2  

dev_dependencies:
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
